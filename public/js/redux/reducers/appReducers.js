import * as types from '../../constants/actionTypes';

const appReducer = (state = {}, action) => {
    switch (action.type) {
        default:
            return state
    }
}


export default appReducer;
